apiVersion: v1
kind: Service
metadata:
  name: p3-svc
  namespace: dev
spec:
  type: NodePort
  selector:
    app: p3-app
  ports:
  - protocol: TCP
    port: 8888
    targetPort: 8888
    nodePort: 30042
